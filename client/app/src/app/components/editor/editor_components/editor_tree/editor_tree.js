import React, { Component } from 'react';
import TreeMenu from 'react-simple-tree-menu';
import {treeData} from './editor_tree_seed';

  class EditorTree extends Component {
    constructor(props) {
        super(props);
        this.state = {  }
    }
    render() { 
        return (
           <h1>t</h1>
             );
    }
}
 
export default EditorTree;